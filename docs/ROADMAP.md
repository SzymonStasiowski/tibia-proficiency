## Roadmap

Sprint A
- Schema: catalog tables (items, imbuements, charms) and `character_builds` (JSON config).
- Scrapers: items + imbuements (dry-run + upsert); media ingestion reusing backfill patterns.
- UI: /build/new MVP with equipment and imbuements; summary-only.

Sprint B
- Scraper: charms + monster target tags UI.
- UI: /build/[slug] share page, copy link.
- Remove voting UI and writes; keep legacy tables for now.

Sprint C
- Catalog browse pages and search.
- Data QA and fixes; prepare Wheel+Gems plan.

